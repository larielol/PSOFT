/*public class ScrumBoard {
    public static void main(String[] args) {
        UserStory userStory = new UserStory(null);

        // Movendo a US para InProgress (qualquer membro da equipe pode fazer isso)
        userStory.mover();
        
        // Movendo a US para ToVerify (apenas desenvolvedores podem fazer isso)
        userStory.mover();

        // Aprovação da US pelo Scrum Master
        userStory.verificar(true);

        // Movendo a US para Done (apenas o Scrum Master pode fazer isso)
        userStory.mover();

    }
}*/
